import React from 'react';
import Chart from 'react-apexcharts';

const BarGraphComp = ({options, series}) => {
    return (
        <Chart options={options} series={series} type="bar" width="100%" height="100%"/>
    );
}

export default BarGraphComp;
